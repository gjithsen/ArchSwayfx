sudo pacman -S --needed base-devel git
git clone https://aur.archlinux.org/swayfx.git
cd swayfx
makepkg -si --noconfirm
```bash
sudo pacman -S --needed base-devel git
git clone https://aur.archlinux.org/swayfx.git
cd swayfx
makepkg -si
```
